export const metadata = {
  title: "Profile Comparison",
  description:
    "Compare your LinkedIn profile and resume to ensure consistency and optimal positioning for your target roles.",
};

export default function CompareLayout({ children }) {
  return <>{children}</>;
}

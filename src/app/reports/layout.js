export const metadata = {
  title: "My Reports",
  description:
    "View and manage all your LinkedIn, resume, and comparison reports in one place.",
};

export default function ReportsLayout({ children }) {
  return <>{children}</>;
}
